import 'package:flutter/material.dart';

class InvestScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Invest')),
      body: Center(
        child: Text('Investment UI goes here'),
      ),
    );
  }
}
